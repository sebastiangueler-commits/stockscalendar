{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "functions": {
    "api/*.py": {
      "runtime": "python@3.9"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
